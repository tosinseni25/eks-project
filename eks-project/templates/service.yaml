apiVersion: v1
kind: Service
metadata:
  name: {{ include "eks-project.fullname" . }}
  labels:
    {{- include "eks-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eks-project.selectorLabels" . | nindent 4 }}
